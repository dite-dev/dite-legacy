import { bootstrap as createServer } from '/Users/loyep/Git/dite/examples/boilerplate/server/index';

(async () => {
  const config = {
    port: 3001,
  }
  await createServer({ config });
  const payload = {
    type: 'dite:ready',
    port: config.port,
    finishTime: Math.ceil(performance.now()),
  };
  process.send?.(payload);
})();
